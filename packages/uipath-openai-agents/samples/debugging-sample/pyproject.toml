[project]
name = "debugging-sample"
version = "0.1.0"
description = "Sample demonstrating breakpoint debugging with OpenAI Agents"
requires-python = ">=3.11"
dependencies = [
    "uipath-openai-agents>=0.1.0, <0.2.0",
    "openai-agents>=0.6.5",
]

[dependency-groups]
dev = []

[tool.uv.sources]
uipath-openai-agents = { path = "../..", editable = true }
